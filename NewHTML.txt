<p><!DOCTYPE html> <html lang=&quot;en&quot;> <head>  <meta charset=&quot;UTF-8&quot;>  <meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;>  <title>Google Docs</title>  <link rel=&quot;icon&quot; type=&quot;image/x-icon&quot; href=&quot;https://gstatic.com&quot;>  <style>  :root {  --bg-gradient: radial-gradient(circle at center, #111 0%, #050505 100%);  --card-bg: rgba(255, 255, 255, 0.05);  --border: rgba(255, 255, 255, 0.1);  --accent: #ffffff;  --glow: rgba(255, 255, 255, 0.4);  }  body { margin: 0; padding: 0; font-family: 'Segoe UI', sans-serif; background: var(--bg-gradient); color: white; display: flex; flex-direction: column; align-items: center; height: 100vh; overflow: hidden; }  .top-nav { position: absolute; top: 0; left: 0; width: 100%; height: 80px; display: flex; justify-content: center; align-items: center; z-index: 10; }  .top-search-bar { width: 350px; padding: 10px 20px; border-radius: 12px; border: 1px solid var(--border); background: var(--card-bg); backdrop-filter: blur(10px); color: white; outline: none; transition: 0.3s; }  .top-search-bar:focus { width: 400px; border-color: rgba(255,255,255,0.3); }  .brand { position: absolute; top: 25px; left: 30px; font-size: 22px; font-weight: 800; opacity: 0.9; z-index: 11; }  .settings-btn { position: absolute; top: 25px; right: 30px; font-size: 24px; cursor: pointer; z-index: 100; opacity: 0.6; }  .settings-panel { position: fixed; top: 70px; right: -310px; width: 250px; background: rgba(10,10,10,0.98); border: 1px solid var(--border); border-radius: 15px; padding: 20px; transition: 0.4s; z-index: 99; }  .settings-panel.open { right: 30px; }  .clock-container { flex-grow: 1; display: flex; flex-direction: column; justify-content: center; align-items: center; margin-top: 40px; }  #clock { font-size: 110px; font-weight: 700; margin: 0; letter-spacing: -4px; background: linear-gradient(180deg, #fff 0%, #888 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }  .clock-line { width: 400px; height: 1.5px; background: linear-gradient(90deg, transparent, var(--accent), transparent); margin: 15px 0; box-shadow: 0 0 12px var(--glow); opacity: 0.8; }  .bottom-area { display: flex; flex-direction: column; align-items: center; gap: 25px; margin-bottom: 80px; }  .search-group { display: flex; background: var(--card-bg); border: 1px solid var(--border); border-radius: 15px; padding: 5px; backdrop-filter: blur(15px); }  .mid-search-bar { width: 350px; padding: 12px 15px; background: transparent; border: none; color: white; outline: none; }  .shortcuts { display: flex; gap: 15px; }  .shortcut-card { background: var(--card-bg); padding: 12px 24px; border-radius: 12px; text-decoration: none; color: rgba(255,255,255,0.8); border: 1px solid var(--border); font-size: 14px; font-weight: 500; transition: 0.3s; cursor: pointer; }  .shortcut-card:hover { transform: translateY(-3px); border-color: white; color: white; }  </style> </head> <body>  <div id=&quot;dashboard&quot; style=&quot;width:100%; height:100%; display:flex; flex-direction:column; align-items:center;&quot;>  <div class=&quot;brand&quot;>@aaashtonfr</div>  <div class=&quot;settings-btn&quot; onclick=&quot;toggleSettings()&quot;>&#9881;&#65039;</div>  <div class=&quot;settings-panel&quot; id=&quot;settingsPanel&quot;>  <h3 style=&quot;margin:0 0 15px 0; font-size:16px;&quot;>Themes</h3>  <div style=&quot;display:flex; gap:10px;&quot;>  <div style=&quot;width:35px; height:35px; border-radius:5px; background:#050505; border:1px solid #333; cursor:pointer;&quot; onclick=&quot;setTheme(0)&quot;></div>  <div style=&quot;width:35px; height:35px; border-radius:5px; background:linear-gradient(#1e3c72, #2a5298); cursor:pointer;&quot; onclick=&quot;setTheme(1)&quot;></div>  <div style=&quot;width:35px; height:35px; border-radius:5px; background:linear-gradient(#ff4e50, #f9d423); cursor:pointer;&quot; onclick=&quot;setTheme(2)&quot;></div>  </div>  </div>  <div class=&quot;top-nav&quot;><input type=&quot;text&quot; class=&quot;top-search-bar&quot; placeholder=&quot;https://...&quot; id=&quot;topSearch&quot;></div>  <div class=&quot;clock-container&quot;>  <h1 id=&quot;clock&quot;>00:00:00</h1>  <div class=&quot;clock-line&quot;></div>  </div>  <div class=&quot;bottom-area&quot;>  <div class=&quot;search-group&quot;><input type=&quot;text&quot; class=&quot;mid-search-bar&quot; placeholder=&quot;Search the web...&quot; id=&quot;midSearch&quot;></div>  <div class=&quot;shortcuts&quot;>  <div onclick=&quot;launch('https://soundcloud.com')&quot; class=&quot;shortcut-card&quot;>Music</div>  <div onclick=&quot;launch('https://discord.com')&quot; class=&quot;shortcut-card&quot;>Discord</div>  <div onclick=&quot;launch('https://github.com')&quot; class=&quot;shortcut-card&quot;>GitHub</div>  </div>  </div>  </div></p>

<p> <script>  function updateClock() { document.getElementById('clock').textContent = new Date().toLocaleTimeString('en-GB'); }  setInterval(updateClock, 1000); updateClock();  function toggleSettings() { document.getElementById('settingsPanel').classList.toggle('open'); }  function setTheme(i) {  const themes = [&quot;radial-gradient(#111, #050505)&quot;, &quot;linear-gradient(135deg,#1e3c72,#2a5298)&quot;, &quot;linear-gradient(135deg,#ff4e50,#f9d423)&quot;];  document.body.style.background = themes[i];  }</p>

<p> // URL encoding logic to hide from simple filters  function uvEncode(str) {  if (!str) return str;  return encodeURIComponent(str.split('').map((char, ind) => ind % 2 ? String.fromCharCode(char.charCodeAt() ^ 2) : char).join(''));  }</p>

<p> // REPLACE THIS with your private gateway URL from Replit/CodeSandbox  const gateway = &quot;https://tomp.app&quot;;</p>

<p> function launch(val) {  let url = val.trim();  if (!url) return;  if (!url.includes('.')) url = &quot;https://google.com&quot; + encodeURIComponent(url);  else if (!url.startsWith('http')) url = 'https://' + url;</p>

<p> const win = window.open('about:blank', '_blank');  if (win) {  win.document.title = &quot;Google Docs&quot;;  const link = win.document.createElement('link');  link.rel = 'icon';  link.href = 'https://gstatic.com';  win.document.head.appendChild(link);</p>

<p> const iframe = win.document.createElement('iframe');  iframe.style.cssText = &quot;position:fixed; width:100vw; height:100vh; top:0; left:0; border:none; background:black;&quot;;  iframe.src = gateway + uvEncode(url);  win.document.body.style.margin = &quot;0&quot;;  win.document.body.appendChild(iframe);  }  }</p>

<p> document.getElementById('midSearch').addEventListener('keypress', (e) => { if (e.key === 'Enter') launch(e.target.value); });  document.getElementById('topSearch').addEventListener('keypress', (e) => { if (e.key === 'Enter') launch(e.target.value); });  </script> </body> </html></p>